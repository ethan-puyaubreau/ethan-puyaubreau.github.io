// Single source of truth for the site's origin. Imported by astro.config.mjs
// (as `site`, for canonical/hreflang/sitemap) and by src/lib/site.ts (as the
// fallback for `Astro.site`). A future custom domain is a one-line change here.
export const SITE_ORIGIN = "https://ethan-puyaubreau.github.io";
